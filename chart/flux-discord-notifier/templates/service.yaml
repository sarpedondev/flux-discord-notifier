apiVersion: v1
kind: Service
metadata:
  name: {{ include "flux-discord-notifier.fullname" . }}
  labels:
    {{- include "flux-discord-notifier.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "flux-discord-notifier.selectorLabels" . | nindent 4 }}

